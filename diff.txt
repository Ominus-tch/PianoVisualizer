diff --git a/PianoVisualizer.vcxproj b/PianoVisualizer.vcxproj
index af575cd..f44657d 100644
--- a/PianoVisualizer.vcxproj
+++ b/PianoVisualizer.vcxproj
@@ -36,7 +36,7 @@
     <IntDir>$(SolutionDir)build\intermediates\</IntDir>
     <IncludePath>$(ProjectDir)\ext;$(IncludePath)</IncludePath>
     <ExternalIncludePath>$(ProjectDir)\ext;$(ExternalIncludePath)</ExternalIncludePath>
-    <PreBuildEventUseInBuild>false</PreBuildEventUseInBuild>
+    <PreBuildEventUseInBuild>true</PreBuildEventUseInBuild>
   </PropertyGroup>
   <ItemDefinitionGroup Condition="'$(Configuration)|$(Platform)'=='Release|x64'">
     <ClCompile>
diff --git a/resources/shaders/particlesblur.frag b/resources/shaders/particlesblur.frag
index 6f8ef22..210045e 100644
--- a/resources/shaders/particlesblur.frag
+++ b/resources/shaders/particlesblur.frag
@@ -1,5 +1,6 @@
 struct PSInput {
     float4 position : SV_Position;
+    float2 uv : TEXCOORD0;
 };
 
 cbuffer ParticlesBlurPixelConstants : register(b0) {
@@ -12,27 +13,37 @@ cbuffer ParticlesBlurPixelConstants : register(b0) {
 Texture2D screenTexture : register(t0);
 SamplerState screenSampler : register(s0);
 
-float2 reconstructUV(float2 screenPosition) {
-    float2 ndc;
-    ndc.x = screenPosition.x * 2.0 * inverseScreenSize.x - 1.0;
-    ndc.y = 1.0 - screenPosition.y * 2.0 * inverseScreenSize.y;
-    return 0.5 * (ndc * 0.5 + 0.5);
-}
+//float4 blur(float2 uv,float vert) {
+//    float4 color = 0.2270270270 * screenTexture.Sample(screenSampler,uv);
+//    float2 pixelOffset = vert > 0.5 ? float2(0.0,inverseScreenSize.y) : float2(inverseScreenSize.x,0.0);
+//    float2 texCoordOffset0 = 1.3846153846 * pixelOffset;
+//    float4 col0 = screenTexture.Sample(screenSampler,uv + texCoordOffset0) + screenTexture.Sample(screenSampler,uv - texCoordOffset0);
+//    color += 0.3162162162 * col0;
+//    float2 texCoordOffset1 = 3.2307692308 * pixelOffset;
+//    float4 col1 = screenTexture.Sample(screenSampler,uv + texCoordOffset1) + screenTexture.Sample(screenSampler,uv - texCoordOffset1);
+//    color += 0.0702702703 * col1;
+//    return color;
+//}
+
+float4 blur(float2 uv, float vert)
+{
+    float2 pixelOffset =
+        vert > 0.5
+        ? float2(0.0, inverseScreenSize.y)
+        : float2(inverseScreenSize.x, 0.0);
+
+    float4 color = 0.0;
+
+    color += screenTexture.Sample(screenSampler, uv) * 0.25;
+    color += screenTexture.Sample(screenSampler, uv + pixelOffset * 5.0) * 0.20;
+    color += screenTexture.Sample(screenSampler, uv - pixelOffset * 5.0) * 0.20;
+    color += screenTexture.Sample(screenSampler, uv + pixelOffset * 10.0) * 0.175;
+    color += screenTexture.Sample(screenSampler, uv - pixelOffset * 10.0) * 0.175;
 
-float4 blur(float2 uv,float vert) {
-    float4 color = 0.2270270270 * screenTexture.Sample(screenSampler,uv);
-    float2 pixelOffset = vert > 0.5 ? float2(0.0,inverseScreenSize.y) : float2(inverseScreenSize.x,0.0);
-    float2 texCoordOffset0 = 1.3846153846 * pixelOffset;
-    float4 col0 = screenTexture.Sample(screenSampler,uv + texCoordOffset0) + screenTexture.Sample(screenSampler,uv - texCoordOffset0);
-    color += 0.3162162162 * col0;
-    float2 texCoordOffset1 = 3.2307692308 * pixelOffset;
-    float4 col1 = screenTexture.Sample(screenSampler,uv + texCoordOffset1) + screenTexture.Sample(screenSampler,uv - texCoordOffset1);
-    color += 0.0702702703 * col1;
     return color;
 }
 
 float4 main(PSInput input) : SV_Target {
-    float2 uv = reconstructUV(input.position.xy);
-    float4 color = blur(uv,time > 0.5 ? 1.0 : 0.0);
+    float4 color = blur(input.uv,time > 0.5 ? 1.0 : 0.0);
     return lerp(float4(backgroundColor,0.0),color,attenuationFactor);
 }
diff --git a/resources/shaders/particlesblur.vert b/resources/shaders/particlesblur.vert
index 677b02f..3b2e641 100644
--- a/resources/shaders/particlesblur.vert
+++ b/resources/shaders/particlesblur.vert
@@ -4,10 +4,12 @@ struct VSInput {
 
 struct VSOutput {
     float4 position : SV_Position;
+    float2 uv : TEXCOORD0;
 };
 
 VSOutput main(VSInput input) {
     VSOutput output;
     output.position = float4(input.v,1.0);
+    output.uv = input.v.xy * 0.5 + 0.5;
     return output;
 }
diff --git a/src/MidiVisualizer/rendering/Renderer.cpp b/src/MidiVisualizer/rendering/Renderer.cpp
index e878a2c..2573b7d 100644
--- a/src/MidiVisualizer/rendering/Renderer.cpp
+++ b/src/MidiVisualizer/rendering/Renderer.cpp
@@ -57,6 +57,27 @@ void Renderer::SetDefaultBlending()
 	);
 }
 
+void Renderer::BindTextureSampler()
+{
+	ID3D11SamplerState* samplers[] =
+	{
+		_linearClampSampler.Get(),
+		_linearClampSampler.Get()
+	};
+
+	_context->VSSetSamplers(
+		0,
+		2,
+		samplers
+	);
+
+	_context->PSSetSamplers(
+		0,
+		2,
+		samplers
+	);
+}
+
 void Renderer::renderSetup(
 	ID3D11Device* device,
 	ID3D11DeviceContext* context
@@ -413,6 +434,38 @@ void Renderer::renderSetup(
 	_texParticles =
 		ResourcesManager::getTextureFor("particles");
 
+	// --------------------------------------------------------
+	// Texture samplers
+	// --------------------------------------------------------
+
+	{
+		D3D11_SAMPLER_DESC desc{};
+		desc.Filter = D3D11_FILTER_MIN_MAG_MIP_LINEAR;
+
+		desc.AddressU = D3D11_TEXTURE_ADDRESS_CLAMP;
+		desc.AddressV = D3D11_TEXTURE_ADDRESS_CLAMP;
+		desc.AddressW = D3D11_TEXTURE_ADDRESS_CLAMP;
+
+		desc.MipLODBias = 0.0f;
+		desc.MaxAnisotropy = 1;
+		desc.ComparisonFunc = D3D11_COMPARISON_NEVER;
+
+		desc.BorderColor[0] = 0.0f;
+		desc.BorderColor[1] = 0.0f;
+		desc.BorderColor[2] = 0.0f;
+		desc.BorderColor[3] = 0.0f;
+
+		desc.MinLOD = 0.0f;
+		desc.MaxLOD = D3D11_FLOAT32_MAX;
+
+		HRESULT hr = _device->CreateSamplerState(
+			&desc,
+			&_linearClampSampler
+		);
+
+		if (FAILED(hr))
+			throw std::runtime_error("Failed to create linear clamp sampler.");
+	}
 
 	// --------------------------------------------------------
 	// Particle texture size
@@ -639,6 +692,8 @@ void Renderer::drawParticles(
 
 	_programParticules.use(_context);
 
+	BindTextureSampler();
+
 	// --------------------------------------------------------
 	// Common uniforms
 	// --------------------------------------------------------
@@ -678,17 +733,20 @@ void Renderer::drawParticles(
 	// Textures
 	// --------------------------------------------------------
 
-	_programParticules.texture(_context,
+	_programParticules.texture(
+		_context,
 		"textureParticles",
 		_texParticles.Get()
 	);
 
-	_programParticules.texture(_context,
+	_programParticules.texture(
+		_context,
 		"textureNoise",
 		_texNoise.Get()
 	);
 
-	_programParticules.texture(_context, 
+	_programParticules.texture(
+		_context,
 		"lookParticles",
 		state.tex
 	);
@@ -828,6 +886,8 @@ void Renderer::drawNotes(
 {
 	_programNotes.use(_context);
 
+	BindTextureSampler();
+
 	// --------------------------------------------------------
 	// Uniforms that are specific to drawing notes
 	// --------------------------------------------------------
@@ -1134,6 +1194,8 @@ void Renderer::drawFlashes(
 
 	_programFlashes.use(_context);
 
+	BindTextureSampler();
+
 	// Uniforms
 	_programFlashes.uniform(
 		"inverseScreenSize",
@@ -1669,6 +1731,8 @@ void Renderer::drawPedals(
 
 	_programPedals.use(_context);
 
+	BindTextureSampler();
+
 	ID3D11Buffer* vertexBuffer = _quadVertices.Get();
 
 	const UINT stride = sizeof(float) * 2;
@@ -1865,6 +1929,8 @@ void Renderer::drawWaves(
 
 	_programWaveNoise.use(_context);
 
+	BindTextureSampler();
+
 	_programWaveNoise.uniform(
 		"keyboardSize",
 		keyboardHeight
@@ -2255,6 +2321,8 @@ void Renderer::drawScore(
 		{
 			_programScoreBars.use(_context);
 
+			BindTextureSampler();
+
 			_programScoreBars.uniform(
 				"baseOffset",
 				glm::vec2(0.0f, firstBarCoord)
@@ -2341,6 +2409,8 @@ void Renderer::drawScore(
 
 			_programScoreLabels.use(_context);
 
+			BindTextureSampler();
+
 			_programScoreLabels.uniform(
 				"baseOffset",
 				glm::vec2(-1.0f, firstBarCoord) +
diff --git a/src/MidiVisualizer/rendering/Renderer.h b/src/MidiVisualizer/rendering/Renderer.h
index b9ae0af..a85508d 100644
--- a/src/MidiVisualizer/rendering/Renderer.h
+++ b/src/MidiVisualizer/rendering/Renderer.h
@@ -139,6 +139,8 @@ public:
     void SetAdditiveBlending();
     void SetDefaultBlending();
 
+    void BindTextureSampler();
+
 private:
 
 
@@ -195,6 +197,9 @@ private:
     ComPtr<ID3D11ShaderResourceView> _texFont;
     ComPtr<ID3D11ShaderResourceView> _texNoise;
 
+    ComPtr<ID3D11SamplerState> _linearClampSampler;
+    ComPtr<ID3D11SamplerState> _linearWrapSampler;
+
     // Cached info.
     unsigned int _minKeyMajor{ 0 };
     unsigned int _keyCount{ 128 };
diff --git a/src/MidiVisualizer/rendering/ScreenQuad.cpp b/src/MidiVisualizer/rendering/ScreenQuad.cpp
index 9bec08c..6028745 100644
--- a/src/MidiVisualizer/rendering/ScreenQuad.cpp
+++ b/src/MidiVisualizer/rendering/ScreenQuad.cpp
@@ -16,10 +16,11 @@ void ScreenQuad::init(
     ID3D11ShaderResourceView* texture,
     const std::string& fragName,
     const std::string& vertName,
-    bool verbose
+    bool verbose,
+    ShaderProgram::InputLayoutType layoutType
 )
 {
-    init(device, fragName, vertName, verbose);
+    init(device, fragName, vertName, verbose, layoutType);
 
     _texture = texture;
 }
@@ -28,7 +29,8 @@ void ScreenQuad::init(
     ID3D11Device* device,
     const std::string& fragName,
     const std::string& vertName,
-    bool verbose
+    bool verbose,
+    ShaderProgram::InputLayoutType layoutType
 )
 {
     _verbose = verbose;
@@ -36,7 +38,7 @@ void ScreenQuad::init(
         device,
         vertName,
         fragName,
-        ShaderProgram::InputLayoutType::Position3D,
+        layoutType,
         verbose
     );
 
@@ -203,7 +205,6 @@ void ScreenQuad::draw(
     if (!context)
         return;
 
-
     _program.use(context);
 
     if (texture)
@@ -215,11 +216,32 @@ void ScreenQuad::draw(
         );
     }
 
+    const UINT stride = sizeof(Vertex);
+    const UINT offset = 0;
+
+    context->IASetVertexBuffers(
+        0,
+        1,
+        _vertexBuffer.GetAddressOf(),
+        &stride,
+        &offset
+    );
+
+    context->IASetIndexBuffer(
+        _indexBuffer.Get(),
+        DXGI_FORMAT_R32_UINT,
+        0
+    );
+
     context->IASetPrimitiveTopology(
         D3D11_PRIMITIVE_TOPOLOGY_TRIANGLELIST
     );
 
-    context->Draw(3, 0);
+    context->DrawIndexed(
+        _indexCount,
+        0,
+        0
+    );
 }
 
 void ScreenQuad::draw(
@@ -229,16 +251,56 @@ void ScreenQuad::draw(
     const glm::vec2& invScreenSize
 )
 {
+    if (!context)
+        return;
+
+    _program.use(context);
+
     _program.uniform(
         "inverseScreenSize",
         invScreenSize
     );
 
-    draw(
-        context,
-        texture,
+    _program.uniform(
+        "time",
         time
     );
+
+    if (texture)
+    {
+        _program.texture(
+            context,
+            "screenTexture",
+            texture
+        );
+    }
+
+    const UINT stride = sizeof(Vertex);
+    const UINT offset = 0;
+
+    context->IASetVertexBuffers(
+        0,
+        1,
+        _vertexBuffer.GetAddressOf(),
+        &stride,
+        &offset
+    );
+
+    context->IASetIndexBuffer(
+        _indexBuffer.Get(),
+        DXGI_FORMAT_R32_UINT,
+        0
+    );
+
+    context->IASetPrimitiveTopology(
+        D3D11_PRIMITIVE_TOPOLOGY_TRIANGLELIST
+    );
+
+    context->DrawIndexed(
+        _indexCount,
+        0,
+        0
+    );
 }
 
 // ------------------------------------------------------------
diff --git a/src/MidiVisualizer/rendering/ScreenQuad.h b/src/MidiVisualizer/rendering/ScreenQuad.h
index 45830c5..a86ef5e 100644
--- a/src/MidiVisualizer/rendering/ScreenQuad.h
+++ b/src/MidiVisualizer/rendering/ScreenQuad.h
@@ -21,14 +21,16 @@ public:
         ID3D11ShaderResourceView* texture,
         const std::string& fragName,
         const std::string& vertName = "screenquad_vert",
-        bool verbose = false
+        bool verbose = false,
+        ShaderProgram::InputLayoutType layoutType = ShaderProgram::InputLayoutType::Position3D
     );
 
     void init(
         ID3D11Device* device,
         const std::string& fragName,
         const std::string& vertName = "screenquad_vert",
-        bool verbose = false
+        bool verbose = false,
+        ShaderProgram::InputLayoutType layoutType = ShaderProgram::InputLayoutType::Position3D
     );
 
     void draw(
@@ -75,6 +77,7 @@ private:
     ComPtr<ID3D11Buffer> _indexBuffer;
 
     ID3D11ShaderResourceView* _texture = nullptr;
+    ID3D11SamplerState* _screenSampler = nullptr;
 
     UINT _indexCount = 0;
 
diff --git a/src/MidiVisualizer/rendering/Viewer.cpp b/src/MidiVisualizer/rendering/Viewer.cpp
index 7c8cb68..190cd0a 100644
--- a/src/MidiVisualizer/rendering/Viewer.cpp
+++ b/src/MidiVisualizer/rendering/Viewer.cpp
@@ -156,9 +156,9 @@ Viewer::Viewer(
 
 	_background.init(_device, "background_vert", "background_frag");
 
-	_blurringScreen.init(_device, _particlesFramebuffer->textureId(), "particlesblur_frag");
-	_fxaa.init(_device, "fxaa_frag");
-	_passthrough.init(_device, "screenquad_frag");
+	_blurringScreen.init(_device, _particlesFramebuffer->textureId(), "particlesblur_frag", "particlesblur_vert", true);
+	_fxaa.init(_device, "fxaa_frag", "screenquad_vert", false, ShaderProgram::InputLayoutType::None);
+	_passthrough.init(_device, "screenquad_frag", "screenquad_vert", false, ShaderProgram::InputLayoutType::None);
 
 	// Create the layers.
 	//_layers[Layer::BGCOLOR].type = Layer::BGCOLOR;
@@ -3509,21 +3509,21 @@ void Viewer::showPedalsEditor(){
 
 void Viewer::applyBackgroundColor()
 {
-	//const float clearColor[4] =
-	//{
-	//	_state.background.color.r,
-	//	_state.background.color.g,
-	//	_state.background.color.b,
-	//	0.0f
-	//};
-
 	const float clearColor[4] =
+	{
+		_state.background.color.r,
+		_state.background.color.g,
+		_state.background.color.b,
+		0.0f
+	};
+
+	/*const float clearColor[4] =
 	{
 		0.1f,
 		0.1f,
 		0.1f,
 		0.0f
-	};
+	};*/
 
 	// Clear particle framebuffer.
 	_particlesFramebuffer->bind(_context);
diff --git a/src/MidiVisualizer/resources/shaders.cpp b/src/MidiVisualizer/resources/shaders.cpp
index bf508e3..fcac0b6 100644
--- a/src/MidiVisualizer/resources/shaders.cpp
+++ b/src/MidiVisualizer/resources/shaders.cpp
@@ -11,8 +11,8 @@ const std::unordered_map<std::string, std::string> shaders = {
 	{ "particles_vert", "struct VSInput {\n     float2 v : POSITION;\n };\n struct VSOutput {\n     float4 position : SV_Position;\n     float2 uv : TEXCOORD0;\n     float id : TEXCOORD1;\n };\n cbuffer ParticlesVertexConstants : register(b0) {\n     float time;\n     float scale;\n     float2 inverseScreenSize;\n     float2 inverseTextureSize;\n     int globalId;\n     float duration;\n     int channel;\n     int texCount;\n     float colorScale;\n     float expansionFactor;\n     float speedScaling;\n     float keyboardHeight;\n     float turbulenceStrength;\n     float turbulenceScale;\n     int minNote;\n     float notesCount;\n     int horizontalMode;\n };\n Texture2D textureParticles : register(t0);\n Texture2D textureNoise : register(t1);\n SamplerState particleSampler : register(s0);\n SamplerState noiseSampler : register(s1);\n static const float shifts[128] = { 0.0,0.5,1.0,1.5,2.0,3.0,3.5,4.0,4.5,5.0,5.5,6.0,7.0,7.5,8.0,8.5,9.0,10.0,10.5,11.0,11.5,12.0,12.5,13.0,14.0,14.5,15.0,15.5,16.0,17.0,17.5,18.0,18.5,19.0,19.5,20.0,21.0,21.5,22.0,22.5,23.0,24.0,24.5,25.0,25.5,26.0,26.5,27.0,28.0,28.5,29.0,29.5,30.0,31.0,31.5,32.0,32.5,33.0,33.5,34.0,35.0,35.5,36.0,36.5,37.0,38.0,38.5,39.0,39.5,40.0,40.5,41.0,42.0,42.5,43.0,43.5,44.0,45.0,45.5,46.0,46.5,47.0,47.5,48.0,49.0,49.5,50.0,50.5,51.0,52.0,52.5,53.0,53.5,54.0,54.5,55.0,56.0,56.5,57.0,57.5,58.0,59.0,59.5,60.0,60.5,61.0,61.5,62.0,63.0,63.5,64.0,64.5,65.0,66.0,66.5,67.0,67.5,68.0,68.5,69.0,70.0,70.5,71.0,71.5,72.0,73.0,73.5,74.0\n };\n float rand(float2 co) {\n     return frac(sin(dot(co,float2(12.9898,78.233))) * 43758.5453);\n }\n float2 flipIfNeeded(float2 inPos) {\n     return horizontalMode != 0 ? float2(inPos.y,-inPos.x) : inPos;\n }\n VSOutput main(VSInput input,uint instanceId : SV_InstanceID) {\n     VSOutput output;\n     output.id = (float)(instanceId % (uint)texCount);\n     output.uv = input.v + 0.5;\n     float localTime = speedScaling * time * duration;\n     float particlesCount = 1.0 / inverseTextureSize.y;\n     float particleId = (float)instanceId + floor(particlesCount * 10.0 * rand(float2((float)globalId,(float)globalId)));\n     float textureId = fmod(particleId,particlesCount);\n     float particleShift = floor(particleId / particlesCount);\n     float2 particleUV = float2(localTime / inverseTextureSize.x + 10.0 * particleShift,textureId);\n     particleUV = (particleUV + 0.5) * inverseTextureSize;\n     particleUV.x = clamp(particleUV.x,0.0,1.0);\n     float3 position = textureParticles.SampleLevel(particleSampler,particleUV,0.0).xyz;\n     position.x -= 0.5;\n     float2 shift = 0.5 * position.xy;\n     float random = rand(float2(particleId + (float)globalId,time * 0.000002 + 100.0 * (float)globalId));\n     shift += float2(0.0,0.1 * random);\n     shift = shift * time * expansionFactor;\n     shift.x *= max(0.5,pow(shift.y,0.3));\n     float xshift = -1.0 + ((shifts[globalId] - shifts[minNote]) * 2.0 + 1.0) / notesCount;\n     float2 globalShift = float2(xshift,(2.0 * keyboardHeight - 1.0) - 0.02);\n     float2 localShift = shift * duration * float2(1.0,0.5);\n     float2 vertexShift = 0.003 * scale * input.v;\n     float screenRatio = inverseScreenSize.y / inverseScreenSize.x;\n     float2 screenScaling = float2(1.0,horizontalMode != 0 ? 1.0 / screenRatio : screenRatio);\n     float2 particlePos = globalShift + screenScaling * localShift;\n     float2 curlNoise = textureNoise.SampleLevel(noiseSampler,turbulenceScale * particlePos.xy / screenScaling,0.0).gb;\n     curlNoise.x = 2.0 * curlNoise.x - 1.0;\n     float2 curlShift = 0.01 * turbulenceStrength * time * curlNoise;\n     float2 finalPos = particlePos + screenScaling * (vertexShift + curlShift);\n     finalPos = lerp(float2(-200.0,-200.0),finalPos,position.z);\n     output.position = float4(flipIfNeeded(finalPos),0.0,1.0);\n     return output;\n }\n "}, 
 	{ "particles_frag", "struct PSInput {\n     float4 position : SV_Position;\n     float2 uv : TEXCOORD0;\n     float id : TEXCOORD1;\n };\n Texture2DArray lookParticles : register(t0);\n SamplerState particleSampler : register(s0);\n cbuffer ParticlesPixelConstants : register(b0) {\n     float time;\n     float colorScale;\n     int channel;    \n float4 baseColor[12];\n }\n float4 main(PSInput input) : SV_Target {\n     float alpha = lookParticles.Sample(particleSampler,float3(input.uv,input.id)).r;\n     float4 color = float4(colorScale * baseColor[channel].rgb,1.0 - time * time);\n     color.a *= alpha;\n     return color;\n }\n "},
 
-	{ "particlesblur_vert", "struct VSInput {\n     float3 v : POSITION;\n };\n struct VSOutput {\n     float4 position : SV_Position;\n };\n VSOutput main(VSInput input) {\n     VSOutput output;\n     output.position = float4(input.v,1.0);\n     return output;\n }\n "}, 
-	{ "particlesblur_frag", "struct PSInput {\n     float4 position : SV_Position;\n };\n cbuffer ParticlesBlurPixelConstants : register(b0) {\n     float2 inverseScreenSize;\n     float3 backgroundColor;\n     float attenuationFactor;\n     float time;\n };\n Texture2D screenTexture : register(t0);\n SamplerState screenSampler : register(s0);\n float2 reconstructUV(float2 screenPosition) {\n     float2 ndc;\n     ndc.x = screenPosition.x * 2.0 * inverseScreenSize.x - 1.0;\n     ndc.y = 1.0 - screenPosition.y * 2.0 * inverseScreenSize.y;\n     return 0.5 * (ndc * 0.5 + 0.5);\n }\n float4 blur(float2 uv,float vert) {\n     float4 color = 0.2270270270 * screenTexture.Sample(screenSampler,uv);\n     float2 pixelOffset = vert > 0.5 ? float2(0.0,inverseScreenSize.y) : float2(inverseScreenSize.x,0.0);\n     float2 texCoordOffset0 = 1.3846153846 * pixelOffset;\n     float4 col0 = screenTexture.Sample(screenSampler,uv + texCoordOffset0) + screenTexture.Sample(screenSampler,uv - texCoordOffset0);\n     color += 0.3162162162 * col0;\n     float2 texCoordOffset1 = 3.2307692308 * pixelOffset;\n     float4 col1 = screenTexture.Sample(screenSampler,uv + texCoordOffset1) + screenTexture.Sample(screenSampler,uv - texCoordOffset1);\n     color += 0.0702702703 * col1;\n     return color;\n }\n float4 main(PSInput input) : SV_Target {\n     float2 uv = reconstructUV(input.position.xy);\n     float4 color = blur(uv,time > 0.5 ? 1.0 : 0.0);\n     return lerp(float4(backgroundColor,0.0),color,attenuationFactor);\n }\n "},
+	{ "particlesblur_vert", "struct VSInput {\n     float3 v : POSITION;\n };\n struct VSOutput {\n     float4 position : SV_Position;\n     float2 uv : TEXCOORD0;\n };\n VSOutput main(VSInput input) {\n     VSOutput output;\n     output.position = float4(input.v,1.0);\n     output.uv = input.v.xy * 0.5 + 0.5;\n     return output;\n }\n "}, 
+	{ "particlesblur_frag", "struct PSInput {\n     float4 position : SV_Position;\n     float2 uv : TEXCOORD0;\n };\n cbuffer ParticlesBlurPixelConstants : register(b0) {\n     float2 inverseScreenSize;\n     float3 backgroundColor;\n     float attenuationFactor;\n     float time;\n };\n Texture2D screenTexture : register(t0);\n SamplerState screenSampler : register(s0);\n //float4 blur(float2 uv,float vert) {\n //    float4 color = 0.2270270270 * screenTexture.Sample(screenSampler,uv);\n //    float2 pixelOffset = vert > 0.5 ? float2(0.0,inverseScreenSize.y) : float2(inverseScreenSize.x,0.0);\n //    float2 texCoordOffset0 = 1.3846153846 * pixelOffset;\n //    float4 col0 = screenTexture.Sample(screenSampler,uv + texCoordOffset0) + screenTexture.Sample(screenSampler,uv - texCoordOffset0);\n //    color += 0.3162162162 * col0;\n //    float2 texCoordOffset1 = 3.2307692308 * pixelOffset;\n //    float4 col1 = screenTexture.Sample(screenSampler,uv + texCoordOffset1) + screenTexture.Sample(screenSampler,uv - texCoordOffset1);\n //    color += 0.0702702703 * col1;\n //    return color;\n //}\n float4 blur(float2 uv, float vert)\n {\n     float2 pixelOffset =\n         vert > 0.5\n         ? float2(0.0, inverseScreenSize.y)\n         : float2(inverseScreenSize.x, 0.0);\n     float4 color = 0.0;\n     color += screenTexture.Sample(screenSampler, uv) * 0.25;\n     color += screenTexture.Sample(screenSampler, uv + pixelOffset * 5.0) * 0.20;\n     color += screenTexture.Sample(screenSampler, uv - pixelOffset * 5.0) * 0.20;\n     color += screenTexture.Sample(screenSampler, uv + pixelOffset * 10.0) * 0.175;\n     color += screenTexture.Sample(screenSampler, uv - pixelOffset * 10.0) * 0.175;\n     return color;\n }\n float4 main(PSInput input) : SV_Target {\n     float4 color = blur(input.uv,time > 0.5 ? 1.0 : 0.0);\n     return lerp(float4(backgroundColor,0.0),color,attenuationFactor);\n }\n "},
 
 	{ "screenquad_vert", "struct VSOutput {\n     float4 position : SV_POSITION;\n     float2 uv : TEXCOORD0;\n };\n VSOutput main(uint vertexID : SV_VertexID)\n {\n     VSOutput output;\n     float2 positions[3] = {\n         float2(-1.0f, -1.0f),\n         float2(-1.0f,  3.0f),\n         float2( 3.0f, -1.0f)\n     };\n     float2 uvs[3] = {\n         float2(0.0f, 1.0f),\n         float2(0.0f, -1.0f),\n         float2(2.0f, 1.0f)\n     };\n     output.position = float4(positions[vertexID], 0.0f, 1.0f);\n     output.uv = uvs[vertexID];\n     return output;\n }\n "}, 
 	{ "screenquad_frag", "Texture2D screenTexture : register(t0);\n SamplerState screenSampler : register(s0);\n struct PSInput {\n     float4 position : SV_POSITION;\n     float2 uv : TEXCOORD0;\n };\n float4 main(PSInput input) : SV_TARGET\n {\n     return screenTexture.Sample(screenSampler, input.uv);\n }\n "},
diff --git a/src/PianoVisualizer.cpp b/src/PianoVisualizer.cpp
index 9d530f2..bb9ed65 100644
--- a/src/PianoVisualizer.cpp
+++ b/src/PianoVisualizer.cpp
@@ -2206,6 +2206,12 @@ void PianoVisualizer::RenderPolygonSelectionTooltip()
         m_choosingPolygonPoints =
             false;
 
+        if (m_temporaryPerspectiveScene)
+        {
+            m_pianoScene = PianoScene::PianoRoll;
+            m_temporaryPerspectiveScene = false;
+        }
+
         m_polygonClickCount =
             static_cast<int>(
                 m_polygonPoints.size()
@@ -3669,6 +3675,12 @@ void PianoVisualizer::HandlePolygonPointSelection(
         m_choosingPolygonPoints =
             false;
 
+        if (m_temporaryPerspectiveScene)
+        {
+            m_pianoScene = PianoScene::PianoRoll;
+            m_temporaryPerspectiveScene = false;
+        }
+
         Logger::Log(
             "Piano corner selection complete.\n"
         );
@@ -3955,7 +3967,7 @@ void PianoVisualizer::RenderDisplayTab()
     // Piano Surface
     // =================================================
 
-    if (m_pianoScene == PianoScene::Perspective)
+    if (m_pianoScene == PianoScene::Perspective && !m_temporaryPerspectiveScene)
     {
         ImGui::Spacing();
         ImGui::Separator();
@@ -4089,14 +4101,67 @@ void PianoVisualizer::RenderDisplayTab()
     // Piano Roll
     // =================================================
 
-    else if (m_pianoScene == PianoScene::PianoRoll)
+    else
     {
         ImGui::Spacing();
         ImGui::Separator();
         ImGui::Spacing();
 
-        ImGui::Text(
-            "PIANO ROLL"
+        ImGui::SeparatorText(
+            "Piano Surface"
+        );
+
+        ImGui::Spacing();
+
+        if (!m_choosingPolygonPoints)
+        {
+            if (ImGui::Button(
+                "Choose Piano Corners",
+                ImVec2(-1.0f, 36.0f)
+            ))
+            {
+                m_savedPolygonPoints =
+                    m_polygonPoints;
+
+                m_polygonPoints.clear();
+
+                m_polygonClickCount = 0;
+
+                m_choosingPolygonPoints =
+                    true;
+
+                m_temporaryPerspectiveScene =
+                    true;
+
+                m_pianoScene = PianoScene::Perspective;
+
+                Logger::Log(
+                    "[Piano Visualizer] Waiting for 4 piano corner points...\n"
+                );
+            }
+        }
+        else
+        {
+            ImGui::PushStyleColor(
+                ImGuiCol_Button,
+                ImVec4(
+                    0.20f,
+                    0.45f,
+                    0.75f,
+                    1.0f
+                )
+            );
+
+            ImGui::Button(
+                "Click 4 piano corners...",
+                ImVec2(-1.0f, 36.0f)
+            );
+
+            ImGui::PopStyleColor();
+        }
+
+        ImGui::TextDisabled(
+            "Piano Roll"
         );
 
         ImGui::Spacing();
@@ -5799,7 +5864,7 @@ void PianoVisualizer::RenderConfigurationTab()
         ImVec2(buttonWidth, 36.0f)
     ))
     {
-        int _scene;
+        int _scene = 0;
 
         if (
             Config::LoadPianoConfig(
diff --git a/src/PianoVisualizer.h b/src/PianoVisualizer.h
index 9daa1e5..230c011 100644
--- a/src/PianoVisualizer.h
+++ b/src/PianoVisualizer.h
@@ -222,6 +222,7 @@ private:
     bool InitializeWindowCapture();
 
     bool InitializeRenderTarget();
+
     bool InitializeCameraOutput();
 
     // =========================================================
@@ -494,6 +495,7 @@ private:
     float m_pianoRollCameraSourceScale = 1.0f;
 
     bool m_choosingPolygonPoints = false;
+    bool m_temporaryPerspectiveScene = false;
 
     int m_polygonClickCount = 0;
 
